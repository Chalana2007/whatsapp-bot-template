module.exports = {
    owner: process.env.OWNER,        // WhatsApp number 94XXXXXXXXX@c.us
    ownerName: process.env.OWNER_NAME, // Owner / customer name
    botName: process.env.BOT_NAME,     // Bot name
    privateChat: false                 // true = private allowed, false = only group
};